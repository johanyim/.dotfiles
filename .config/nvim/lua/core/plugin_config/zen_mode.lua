local zen_mode = require("zen-mode");



zen_mode.setup{
    window = {
        backdrop = 0,
        width = 80,
        height = 1,
        options = {
            number = false,
        }
    }
}
